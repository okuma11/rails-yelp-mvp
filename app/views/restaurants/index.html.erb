<h1>Restaurant list</h1>
<% @restaurants.each do |restaurant| %>
<ul>
  <li><%= restaurant.name %>:<%= link_to "Show", restaurant_path(restaurant) %>
</li>
</ul>
<% end %>


<%= link_to "Add Restaurant", new_restaurant_url %>
